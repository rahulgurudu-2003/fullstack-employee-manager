# Frontend Public Assets Directory

Any image file placed here can be served directly from the root path of the application.

### How to use:
If you place `login-bg.png` directly in this `public` folder, you can access it in your React files using:
```tsx
<img src="/login-bg.png" alt="Login Graphic" />
```
(No import statement is required!)
